<%= form_with(model: post, html: {name: "post[sub_posts_attributes][#{local_assigns[:index]}]" }) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <tr>
    <tbody class="post-form">
      <td>
        <%= form.select :dev , ['Phillippe', 'Mauricio', 'Rodrigo', 'Antonio', 'Henry', 'Enzo', 'Jasiel', 'Diego'] , {}, { :class => 'btn border dropdown-toggle w-100' }%>
      </td>
      <td>
        <%= form.text_area :description, class: "form-control custom-textarea" %>
      </td>
      <td>
        <%= form.select :good_bad, ['Boa', 'Ruim'], {}, { :class => 'btn border dropdown-toggle w-100' }%>
      </td>
      <td>
        <%= form.select :status, ['Continuar', 'Parar', 'Melhorar'], {}, { :class => 'btn border dropdown-toggle w-100' }%>
      </td>
      <td>
        <%= form.date_field :date , class:"form-control w-100 data", :value => Time.now.strftime('%Y-%m-%d')%>
      </td>
    </tbody>
  </tr>
<% end %>

  